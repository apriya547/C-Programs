#include<stdio.h>
#include<conio.h>
void main()
{
    unsigned int distance;
    printf("enter the distance");
    scanf("%u",&distance);
    printf("the distance is:%u",distance);
    getch();

}
